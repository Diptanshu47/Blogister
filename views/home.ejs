<%-include('partials/header'); %>
    <h1>Home</h1>
    <p><%= home %></p>

    <% content.forEach(function(item){ %>
        <h1> <%= item.name; %>  </h1>
        <p> <%= item.content.substring(0, 99)+" ..." %> <a href="/posts/<%=item._id%>">Read-More</a></p>
    <% }); %>

    <div class="compose">
        <a href="/compose"><button class="btn btn-primary button">📝 Compose</button></a>
    </div>

<%-include('partials/footer'); %>
